# Agent Dockerfile for local builds
# Uses pre-built binary from build/ directory
# Using distroless/base-debian12 - includes ca-certificates + libc for DNS (34.3MB)
# Note: "base" (not "static") required for DNS resolution in Go's net/http
FROM gcr.io/distroless/base-debian12:latest

# Create app directory
WORKDIR /root/

# Copy pre-built binary
COPY build/fluidity-agent .

# Expose proxy port
EXPOSE 8080

# Command to run
ENTRYPOINT ["./fluidity-agent"]
